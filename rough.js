//forEach() function calls a function for each element in an array.

const numbers = [1, 2, 3, 4, 5];

// numbers.forEach(function(number) {
//   number * 2; // Output: 2, 4, 6, 8, 10
// });


numbers.forEach(number => console.log(number*2))